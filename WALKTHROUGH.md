# Журнал змін (Walkthrough)

## Версія: v0.5.5 (Оновлення моделей OpenRouter та повна українізація меню налаштувань)

### Зміни:
1. **Підключено точні найновіші моделі (`src/config.py` та `src/services/manager.py`):**
   - 🔴 **Gemini 3.7 Flash:** `google/gemini-3.7-flash` (розумна модель нового покоління)
   - 🔴 **Gemini 3.5 Flash Lite:** `google/gemini-3.5-flash-lite` (надшвидка та економна)
   - 🟢 **OpenAI GPT-5.6 Luna:** `openai/gpt-5.6-luna` (найновіша модель аналізу та перекладу від OpenAI)
   - 🔷 **DeepSeek V4 Flash:** `deepseek/deepseek-v4-flash-0731` (найновіша Flash модель DeepSeek)

2. **Повна українізація меню налаштувань (`src/handlers/settings.py`):**
   - Усі заголовки, підказки, кнопки та описи меню вибору перекладача та асистента перекладено українською мовою.

3. **Тестування:**
   - Всі 33 тести успішно виконані паралельно (`pytest -n auto`).

---

## Версія: v0.5.4 (Управління життєвим циклом пам'яті асистента в межах сесії)
- Автоматичне скидання пам'яті при переході на режим прямого перекладу.

---

## Версія: v0.5.3 (Конвертація Markdown у Telegram HTML для жирного шрифту та форматування)
- Конвертер `markdown_to_telegram_html`.
