# Журнал змін (Walkthrough)

## Версія: v0.6.4 (Додано кнопку «Скинути пам'ять» у меню налаштувань)

### Зміни:
1. **Кнопка скидання пам'яті в налаштуваннях (`src/keyboards/inline.py`, `src/handlers/settings.py`):**
   - У меню `/settings` при увімкненому режимі асистента (`💡 Режим асистента`) з'являється окрема кнопка **`🗑️ Скинути пам'ять`**.
   - При натисканні історія діалогу поточної сесії асистента миттєво очищається, і користувачеві виводиться спливаюче підтвердження в Telegram: *«🗑️ Пам'ять асистента успішно очищено!»*.

2. **Тестування:**
   - Оновлено `tests/test_keyboards.py`.
   - Всі 35 тестів успішно виконані паралельно (`pytest -n auto`).

---

## Версія: v0.6.3 (Відображення зворотного верифікаційного перекладу в блоці цитування <blockquote>)
- Оформлення зворотного перекладу як нативної цитати.

---

## Версія: v0.6.2 (Підтримка цитат <blockquote> та емодзі-заголовків у повідомленнях асистента)
- Нативні blockquotes та заголовки з емодзі.
