# Журнал змін (Walkthrough)

## Версія: v0.6.3 (Відображення зворотного верифікаційного перекладу в блоці цитування <blockquote>)

### Зміни:
1. **Оновлено форматування зворотного перекладу (`src/handlers/translation.py`):**
   - Блок `🔍 Зворотний переклад (верифікація):` тепер огортається в нативний Telegram-тег `<blockquote>...</blockquote>`.
   - Це забезпечує візуальну чіткість: вгорі клікабельний код перекладу `<code>`, а внизу — акуратна цитата зі зворотним перекладом українською мовою.

2. **Тестування:**
   - Всі 35 тестів успішно виконані паралельно (`pytest -n auto`).

---

## Версія: v0.6.2 (Підтримка цитат <blockquote> та емодзі-заголовків у повідомленнях асистента)
- Нативні blockquotes та заголовки з емодзі.

---

## Версія: v0.6.1 (Виправлення експорту language_normalizer та DEEPL_VALID_TARGET_CODES)
- Виправлено експорт синглтона.
